# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AudioSession' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
   pod 'Masonry', '~> 1.0.2'
   pod 'AKSpeech', '~> 0.0.7'

  # Pods for AudioSession

  target 'AudioSessionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AudioSessionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
